package fiuba.algo3.algochess.Controlador;

public class MoverUnidadSeleccionada {
    // recibira la posicion inicial y la unidad, junto con el jugador y el tablero, para que se llame al metodo tablero.moverUnidad:
    // hay que actualizar la interfaz para que tableroInterfaz comozca los cambios

    //Al final, ver que se pase el turno y el ciclo vuelva a comenzar



    /* --- Luego de que se completa el movimiento, se debe recargar la escena con los cambios realizados ---

        ESTO ES LO QUE CARGA LA ESCENA NUEVA CUANDO ARRANCA EL JUEGO: IMITAR? REFRESH DE JUANI?

        VBox acciones = crearBotonesAccion();
        VBox mensajes = mensajesJugadores(listaJugadores.get(0));
        BorderPane interfaz = InterfazJuego.crearInterfaz(tableroInterfaz.getTableroInterfaz(),mensajes,acciones);

        Scene scene = new Scene(interfaz);
        ventana.setScene(scene);
        ventana.show();

     */
}
